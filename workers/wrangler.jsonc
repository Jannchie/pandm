{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "pandm",
  "main": "src/index.ts",
  "compatibility_date": "2025-09-01",
  "routes": [{ "pattern": "pandm.jannchie.com", "custom_domain": true }],
  // dashboard SPA — build it first: pnpm -C ../web build
  "assets": {
    "directory": "../src/pandm/static",
    "not_found_handling": "single-page-application",
    "run_worker_first": ["/api/*"]
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "pandm",
      "database_id": "54cfe04c-38d9-4552-8143-25b2f5ea5bf6",
      "migrations_dir": "migrations"
    }
  ],
  "r2_buckets": [
    {
      "binding": "MEDIA",
      "bucket_name": "pandm-media"
    }
  ],
  "observability": { "enabled": true }
  // secrets (set via `wrangler secret put`):
  //   GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET — GitHub OAuth App
  //   PANDM_SECRET_KEY — session signing secret (e.g. `openssl rand -hex 32`)
}
